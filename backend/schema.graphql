schema {
    query: Query
    mutation: Mutation
    subscription: Subscription
}

type Query{
    getMessage(id: String): Message
}

type Mutation{
    addMessage(prompt: String): Message
}

type Subscription {
    onMessage: Message @aws_subscribe(mutations: ["addMessage"])
}

type Message{
    response: String
    createdAt: String
}